import {FC} from "react"

const Button: FC = () => {
    return(
        <button style={{backgroundColor: "#4CAF50", color: "white", padding: "5px", border: "none", cursor: "pointer"}}>Button</button>
    )
}

export default Button;